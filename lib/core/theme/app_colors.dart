import 'package:flutter/material.dart';

class AppColors {
  // Brand colors aligned with Laravel admin
  static const primaryBlue = Color(0xFF055498);
  static const secondaryBlue = Color(0xFF123A60);
  static const accentRed = Color(0xFFCE2028);
  static const accentYellow = Color(0xFFFBD116);
  static const accentPurple = Color(0xFF7C3AED);

  // Backgrounds
  static const lightBackground = Color(0xFFF9FAFB);
  static const darkBackground = Color(0xFF0F172A);
  static const cardLight = Color(0xFFFFFFFF);
  static const cardDark = Color(0xFF1E293B);

  // Text
  static const textPrimaryLight = Color(0xFF0A0A0A);
  static const textSecondaryLight = Color(0xFF374151);
}

